import '../styles/global.css';  // Import the global CSS here

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;  // Render the page component
}

export default MyApp;  // Default export of the React component
